{
  "name": "openai-proxy-server",
  "version": "1.0.0",
  "description": "OpenAI proxy server for browser CORS workaround",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.1",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
